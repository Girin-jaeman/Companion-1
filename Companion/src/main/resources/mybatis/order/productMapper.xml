<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product">
  	<select id="ProductSelectAll" resultType="productBean">
  		select * from `product`
  	</select>
  	<select id="ProductDetailOne" resultType="productBean" parameterType="String">
		select * from `product` where prdt_id=#{value}
	</select>
 </mapper>
	<!--<select id="selectProductDetail" resultType="productDetailBean">
		select (a.상품명) (b.가격 수량 가격 상품코드 주문번호) from `product` (별명) left outer join `order_detail` (별명)
			where 상품명=#{value}
	</select> -->
  