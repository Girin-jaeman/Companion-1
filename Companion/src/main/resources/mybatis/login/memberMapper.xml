<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="member">
	<insert id="insertOne" parameterType="memberBean">
		insert into member (mb_id,mb_pw,mb_name,mb_tel,mb_phone,mb_email,mb_addr1,mb_addr2,mb_addr3,mb_code) values (#{mb_id},#{mb_pw},#{mb_name},#{mb_tel},#{mb_phone},#{mb_email},#{mb_addr1},#{mb_addr2},#{mb_addr3},#{mb_code})
	</insert>
</mapper>